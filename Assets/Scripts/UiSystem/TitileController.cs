using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class TitileController : MonoBehaviour
{
    [SerializeField] GameObject BottunObj = null;
    // Start is called before the first frame update
    void Start()
    {
        BottunObj = this.gameObject;

    }

    // Update is called once per frame
    void Update()
    {
        
    }
}
